package Item;

public class ItemFactory
{
	/*public static Storable getStorable(String type, int power)
	{
		if(power < 1)
		{
			System.out.println("Power of " + power + " is not a valid amount");
			return null;
		}
		//Checking if it is a weapon type
		switch(type)
		{
			case "Bow":
				return new Bow(power);
			case "Dagger":
				return new Dagger(power);
			case "Hammer":
				return new Hammer(power);
			case "Staff":
				return new Staff(power);
			case "Sword":
				return new Sword(power);
			default:
		}

		//checking if it is an armor type
		switch(type)
		{
			case "Chainmail":
				return new Chainmail(power);
			default:
		}

		//Checking if it is a consumable type
		switch(type)
		{
			case "Healing":
				return new Healing(power);
			default:
		}

		System.out.println(type + " not recognized. No item of type Storable could be found to match.");
		return null;
	}*/
}